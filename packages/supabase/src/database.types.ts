// This is a placeholder. Run `pnpm db:types` to generate the real types from your Supabase schema.
// Using `any` temporarily so all queries compile while waiting for generated types.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
